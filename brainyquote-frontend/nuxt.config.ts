// nuxt.config.ts
export default {
  vite: {
    resolve: {
      alias: {
        // Optionnel : tu peux forcer la version navigateur d'Axios
        //'axios': 'axios/dist/axios.min.js'
      }
    }
  }
}
